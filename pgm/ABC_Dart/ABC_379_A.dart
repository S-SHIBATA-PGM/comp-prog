// import 'dart:collection';
import 'dart:io';
// import 'dart:math';

void main() {
  final S = stdin.readLineSync()!;
  const String space = ' ';
  const int one = 1;
  const int two = 2;
  const int zero = 0;
  print("${S[one]}${S[two]}${S[zero]}${space}${S[two]}${S[zero]}${S[one]}");
}
