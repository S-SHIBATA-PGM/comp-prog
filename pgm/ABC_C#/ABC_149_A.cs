using System;
// using System.Collections.Generic;
// using System.Linq;

class Program
{
    static void Main()
    {
        String[] arr = Console.ReadLine().Split();
        String S, T;
        S = arr[0];
        T = arr[1];
        Console.WriteLine(T.ToString() + S.ToString());
        return;
    }
}